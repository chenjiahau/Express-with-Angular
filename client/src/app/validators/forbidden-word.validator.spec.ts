import { ForbiddenWordValidator } from './forbidden-word.validator';

describe('ForbiddenWordValidator', () => {
  it('should create an instance', () => {
    expect(new ForbiddenWordValidator()).toBeTruthy();
  });
});
