import { AllowEmailValidator } from './allow-email.validator';

describe('AllowEmailValidator', () => {
  it('should create an instance', () => {
    expect(new AllowEmailValidator()).toBeTruthy();
  });
});
