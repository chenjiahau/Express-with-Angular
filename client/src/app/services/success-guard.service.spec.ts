import { SuccessGuardService } from './success-guard.service';

describe('SuccessGuardService', () => {
  it('should create an instance', () => {
    expect(new SuccessGuardService()).toBeTruthy();
  });
});
