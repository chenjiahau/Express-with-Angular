import { FormStatusDirective } from './form-status.directive';

describe('FormStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new FormStatusDirective();
    expect(directive).toBeTruthy();
  });
});
